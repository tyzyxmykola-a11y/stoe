# Rebuild cycle 20260906T114634Z_280e4b53

Status: **REJECTED; NOT ACTIVATED**

This was a real local-Ollama cycle, not a mock. The supervisor used
`qwen3-coder:latest` at digest
`06c1097efce0431c2045fe7b2e5108366e43bee1b4603a7aded8f21689e90bca`.
The protected evaluator and case hashes remained unchanged.

## Investigation

The active v1 selector passed 0/3 public diagnostic probes. In each case its
goal-only lexical ranking missed the required IP:

| Probe | v1 selected | Required | SToE navigator reached |
| --- | --- | --- | --- |
| raised storage ceiling | `DIAG_STORAGE_DISTRACTOR` | `DIAG_STORAGE_FAILED` | `DIAG_STORAGE_FAILED` |
| unchanged offline constraint | `DIAG_OFFLINE_GENERIC` | `DIAG_OFFLINE_LOCAL` | `DIAG_OFFLINE_LOCAL` |
| measured evidence | `DIAG_EVIDENCE_IDEA` | `DIAG_EVIDENCE_EVAL` | `DIAG_EVIDENCE_EVAL` |

The trace therefore established an actual selector limitation and showed that
the available graph contained useful connections the selector did not use.

## Generated hypothesis and source

The model proposed a broad “dynamic validation mechanism for information
functions.” This was weakly connected to the observed source behavior. Its
generated source did read more observer fields, but tested whole-phrase substring
matches and retained goal-only tokens. It did not correctly distinguish a still
active rejection condition from an invalidated one, and it did not prioritize
relevant measured evaluations.

- Active source SHA-256: `7f4b5aa6b50b4f504915792b88b681b68cf6864f669f4504876a9bea3406d56c`
- Candidate source SHA-256: `3b5351f259ee8137eff6fc7a1337c78211af937aeb03e8998be5b4afdeaee44a`

## Protected evidence and decision

Both the active selector and candidate passed 1/5 held-out cases. Both failed the
critical active-privacy rejection case. The candidate introduced no regression
because the only active pass remained passing, but it produced no strict
improvement and retained a critical failure. The fixed rule therefore rejected
it before activation. The active pointer remained `v1`.

This failure revealed a supervisor-level weakness: abstract seed material could
out-rank explicit missed-connection observations in synthesis, and the proposal
schema did not require source-grounded diagnostic citations. That infrastructure
issue is corrected separately; the protected cases and acceptance rule are not
changed.
