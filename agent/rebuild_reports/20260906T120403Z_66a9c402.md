# Rebuild cycle 20260906T120403Z_66a9c402

Status: **REJECTED; NOT ACTIVATED**

This was a real local-Ollama cycle using `qwen3-coder:latest`, digest
`06c1097efce0431c2045fe7b2e5108366e43bee1b4603a7aded8f21689e90bca`.
It used no paid or cloud API. The protected evaluator and protected cases were
not shown to the model and retained their preregistered hashes.

## Observed investigation

The active `v1` selector passed 0/3 public diagnostic cases. These were executed
before a change was proposed; the limitation was therefore observed rather than
inferred only from source inspection.

| Case | Active selection | Required IP | Bounded navigator selection |
| --- | --- | --- | --- |
| raised storage ceiling | `DIAG_STORAGE_DISTRACTOR` | `DIAG_STORAGE_FAILED` | `DIAG_STORAGE_FAILED` |
| unchanged offline constraint | `DIAG_OFFLINE_GENERIC` | `DIAG_OFFLINE_LOCAL` | `DIAG_OFFLINE_LOCAL` |
| measured evidence | `DIAG_EVIDENCE_IDEA` | `DIAG_EVIDENCE_EVAL` | `DIAG_EVIDENCE_EVAL` |

The report JSON records each observer state, candidate IP, missed reference,
selected and unselected navigation candidate, path, typed edge, direction,
component score, and final score. The synthesis observer state has directed
`depends_on` edges to the three observed failure IPs, making the route from
SToE operation to the research prompt inspectable.

## Generated hypothesis and implementation

The model attributed the misses to the lexical selector ignoring typed graph
relations and proposed relation-weighted ranking. Its candidate passed the
static authority gate, but the implementation incorrectly treated each item's
`failure_condition` prose as if it contained edge-type labels. The selector
contract supplies observer and item records; it does not supply navigator path
edges in that field. Consequently all configured edge weights were zero on the
protected cases and the intended mechanism was not implemented.

- Active source SHA-256: `7f4b5aa6b50b4f504915792b88b681b68cf6864f669f4504876a9bea3406d56c`
- Candidate source SHA-256: `a04cc79684a2b208cad5620095772af8c44bdd52165e33588fb63f9e7fc3c981`
- Rejected source: `../rejected_candidates/20260906T120403Z_66a9c402_candidate.py`

## Protected evidence and decision

The active selector passed 1/5 protected cases. The candidate also passed 1/5,
and both failed the critical `active_privacy_rejection` case. The candidate did
not regress the one baseline pass, but it neither strictly improved pass count
nor cleared critical failures. The fixed rule therefore returned `REJECT`
before activation. The active pointer remains `v1`.

This result rejects this candidate; it does not establish that typed topology
cannot improve selection. It also identifies an unresolved architectural choice:
future work must either expose a bounded, audited structural signal through the
owned component contract or formulate a change that uses only the fields the
selector actually receives. That choice was not made after seeing these results.
