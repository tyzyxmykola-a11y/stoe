# Rebuild cycle 20260906T125305Z_92e7e913

Status: **ACCEPTED AND ACTIVATED; PARTIAL IMPROVEMENT**

This was one real local-Ollama cycle using `qwen3-coder:latest`, digest
`06c1097efce0431c2045fe7b2e5108366e43bee1b4603a7aded8f21689e90bca`.
No paid or cloud API was used. The model did not receive protected cases,
expected protected answers, or evaluation outcomes.

## Infrastructure state before the cycle

The immediately preceding rejected candidate had tried to consume graph-edge
semantics through `failure_condition`, even though that field is natural-language
rejection-basis text. In response, the supervisor added a public input-feasibility
contract and a disclosed behavioral gate. The latter can stop a no-op before
protected evaluation, but cannot cause activation. Protected evaluation and the
existing acceptance rule remained decisive.

Protected hashes remained:

- cases: `76512fc8fda0c0ae31e47c9ba0d8fb6e2555c27f531b0700221673b506444138`
- evaluator: `6be6246238aedb0c37a72be6e2623772ba6ff1b67415fa46ab3ab4272e3fec06`

## SToE-guided investigation and proposal

The active `v1` selector again failed all three disclosed diagnostics. The shared
field retained the observer states, missed IPs, failure conditions, evaluations,
typed paths, directions, candidate scores, and synthesis retrieval. The model
then proposed using only enumerated public selector inputs—observer state plus
item content, provenance, kind, outcome, failure condition, order, and budgets.

The generated source gave modest fixed boosts to supported outcomes and evaluation
kinds. Although its proposal discussed invalidated failures, its actual code did
not correctly reactivate rejected items under changed constraints. That difference
between proposed and implemented mechanism is preserved rather than rationalized
away.

Candidate SHA-256:
`d37f48335f87c4c4370ea78d7719cf3a79aa26ef41b0911c81f448f7828d2cb1`

## Disclosed mechanism check

| Diagnostic | `v1` | Candidate | Candidate selection |
| --- | ---: | ---: | --- |
| raised storage ceiling | fail | fail | `DIAG_STORAGE_EVAL` |
| unchanged offline constraint | fail | fail | `DIAG_OFFLINE_GENERIC` |
| measured evidence | fail | pass | `DIAG_EVIDENCE_EVAL` |

The candidate improved 0/3 to 1/3 with no disclosed regression, so it was allowed
to proceed. This gate is explicitly insufficient for adoption because the model
had already seen these diagnostics.

## Protected evidence

| Protected case | Critical | `v1` | Candidate |
| --- | ---: | ---: | ---: |
| expired compute rejection | no | fail | fail |
| active privacy rejection | yes | fail | pass |
| observed replication evidence | no | fail | pass |
| budget skips oversize | yes | pass | pass |
| changed license reopens tool | no | fail | fail |

The candidate improved 1/5 to 3/5, preserved the active selector's only pass,
and had no critical failure. The supervisor therefore accepted it under the fixed
rule. A fresh worker process verified the source hash, loaded the persistent field
and canonical seed identity, and successfully selected `HEALTH_EVAL` before the
new pointer remained active.

## Interpretation and remaining limitation

This is a real, bounded, agent-generated improvement to an agent-owned component.
The research decision is connected to SToE operations through the stored observer
states, failures, typed navigation traces, bounded synthesis context, generated
hypothesis, implementation IP, matched evaluation IP, and activation state-change
IP.

It is not evidence that the selector itself implements topological navigation.
Its measured gain comes from simple outcome/kind metadata prioritization. It still
fails both protected changed-constraint cases and two of three public diagnostics.
Further improvement should be treated as a new hypothesis and evaluated on fresh
evidence rather than inferred from this acceptance alone.
